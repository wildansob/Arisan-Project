import {StyleSheet, Text, ScrollView} from 'react-native';
import React from 'react';
import CardKocokArisan from '../../Component/CardKocokArisan';

const KocokArisan = () => {
  return (
    <ScrollView>
      <CardKocokArisan />
    </ScrollView>
  );
};

export default KocokArisan;

const styles = StyleSheet.create({});
